To build this first install node.js 
then install vite in command line with 
```npm install -D vite```
change directory to frontend with 
```cd frontend```
run in command line
```npm install```
change director to backend with 
```cd ../backend```
run in command line 
```npm install```
then run with 
```npm run dev```
and click on the locally generated link
